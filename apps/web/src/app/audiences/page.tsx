import { AudiencesClient } from '@/components/audiences/audiences-client';

export const metadata = { title: 'Audiences — Trackfluence' };

export default function AudiencesPage() {
  return <AudiencesClient />;
}
