import { ComplianceClient } from '@/components/compliance/compliance-client';

export const metadata = { title: 'Compliance — Trackfluence' };

export default function CompliancePage() {
  return <ComplianceClient />;
}
