import { CustomersClient } from '@/components/customers/customers-client';

export const metadata = { title: 'Customers — Trackfluence' };

export default function CustomersPage() {
  return <CustomersClient />;
}
